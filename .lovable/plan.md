Plan to fully replace this project with [Experimentation](/projects/006e03ba-ab1a-42e0-991d-9f881117c2d2) and include env files

1. Confirm destination and source
   - Destination: this current project (connected to the main GitHub repo).
   - Source: project `006e03ba-ab1a-42e0-991d-9f881117c2d2` (Experimentation).
   - Cross-project tools are read-only from source → destination; GitHub sync happens automatically after writes.

2. Files to copy (overwrite)
   - Root: `index.html`, `package.json`, `README.md`, `.nvmrc`, `components.json`, `eslint.config.js`, `postcss.config.js`, `tailwind.config.ts`, `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`, `vite.config.ts`.
   - Env files (per your request): `.env`, `.env.example`, `.env.production` — copied from Experimentation, overwriting current values.
   - Entire `public/` tree.
   - Entire `src/` tree, including `src/integrations/supabase/client.ts` and `src/integrations/supabase/types.ts` (so the code matches Experimentation exactly).
   - `supabase/functions/**` from Experimentation.

3. Files NOT copied
   - `supabase/config.toml` (auto-generated per project — leave the current one).
   - `bun.lock` / `node_modules/` (regenerated by install).
   - `.git/`, `.lovable/`, `.workspace/`.

4. Prune old Outworx leftovers
   - For each mirrored area (`src/**`, `public/**`, `supabase/functions/**`, root config files), delete any file in this project that does not exist at the same path in Experimentation, so the trees match exactly and no dead Outworx code remains.

5. `.gitignore` handling (your follow-up)
   - After copying, update `.gitignore` to ignore env files going forward:
     - `.env`
     - `.env.*.local`
     - `.env.local`
     - Keep `.env.example` tracked (committed as the template).
   - Note: copying the env files into the project still writes them to disk and syncs to GitHub on that write. The `.gitignore` rules only prevent *future* changes to those files from being tracked; they do not retroactively remove already-synced files. If you want the env values to never live in the repo, say the word and I'll instead seed values into Lovable Cloud secrets (via the secrets tools) rather than committing them.

6. Verify
   - Run `bun install` to reconcile `package.json` / lock.
   - Run TypeScript build check.
   - Open `/` in preview and confirm the Experimentation hero/sections render (not the old Outworx layout). Restart the dev server once if the preview looks stale.

### Technical notes / risks
- Overwriting `.env` and the Supabase client/types means the destination project will point at whatever backend Experimentation points at. If Experimentation uses a different Lovable Cloud instance, calls from this project will hit that instance. Confirm that's intended; if not, I should skip `.env*`, `src/integrations/supabase/client.ts`, `src/integrations/supabase/types.ts`, and `supabase/config.toml` like before.
- `.env.production` typically contains build-time `VITE_*` keys baked into the bundle on the next deploy — overwriting it changes what the published site talks to.