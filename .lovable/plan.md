Remove the "Minimum 25 client subscriptions required across any mix of plans" line from `src/pages/Pricing.tsx` (around line 115). It's hardcoded frontend copy shown only on the Accountants & Bookkeepers tab — no backend or API change required. Surrounding spacing will be adjusted so the tabs sit cleanly above the plan cards.